extends layouts/main
include mixins/videoBlock

block content
  .home-videos
    each item in videos
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })
      +videoBlock({
        id: item.id,
        videoFile: item.fileUrl,
        title: item.title,
        views: item.views
      })